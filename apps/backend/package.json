{
  "name": "@hotel-project/backend",
  "version": "1.0.0",
  "private": true,

  "scripts": {
    "dev": "next dev src/server",
    "build": "next build src/server",
    "start": "next start src/server",

    "prisma:generate": "prisma generate --schema src/prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema src/prisma/schema.prisma",
    "prisma:reset": "prisma migrate reset --schema src/prisma/schema.prisma"
  },

  "dependencies": {
    "@hotel-project/domain": "^1.0.0",

    "@prisma/client": "^6.0.1",
    "prisma": "^6.0.1",

    "next": "^15.1.6",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },

  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^20.12.7",
    "@types/react": "^19.0.0"
  }
}
